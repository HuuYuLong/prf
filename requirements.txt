# Core dependencies for the PRF release.
# Versions match the conda environment used to run the paper's experiments
# (Windows + CUDA 11.8, Python 3.10, torch 2.2.2+cu118).
#
# Install PyTorch with CUDA first (match your CUDA version), e.g.:
#   pip install torch==2.2.2 torchvision==0.17.2 torchaudio==2.2.2 torchtext==0.17.2 \
#       --index-url https://download.pytorch.org/whl/cu118
# then:
#   pip install -r requirements.txt

torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
torchtext==0.17.2

pytorch-lightning==2.5.2
torchmetrics==1.4.0.post0
hydra-core==1.3.2
omegaconf==2.3.0
einops==0.7.0
numpy==1.26.4
scipy==1.12.0
pandas==2.2.1
scikit-learn==1.4.2
pillow==10.3.0
datasets==2.18.0
matplotlib==3.8.3
tqdm
wandb==0.16.6
prettytable==3.10.0
rich==13.7.1
PyYAML==6.0.1
imageio==2.34.0
timm==0.8.17.dev0
spikingjelly==0.0.0.0.14
thop==0.1.1.post2209072238

# Optional (only needed for some baseline models / dataloaders / visualization):
# seaborn==0.13.2            # hippo kernel visualizations
# transformers==4.40.1       # LM vocabulary (OpenAIVocab), language-modeling tasks only
# natsort==8.4.0             # audio dataloader
# opencv-python==4.9.0.80    # video dataloaders
# cupy-cuda11x==13.1.0       # optional CUDA kernels for spikingjelly / unicornn baseline
# pykeops                    # optional fast Cauchy kernel for S4 (code falls back to naive implementation)
# fast-transformers          # performer attention baseline
# torchdiffeq, torchdyn      # ODE-RNN baselines
# pl_bolts, pytorchvideo     # video/imagenet dataloaders
# segmentation-models-pytorch
